'use client';

export default function LiveProjectButton() {
  return (
    <div
      className="inline-block rounded-full border-2 border-[#D7E2EA] text-[#D7E2EA] font-medium uppercase tracking-[0.2em] px-8 py-3 sm:px-10 sm:py-3.5 text-xs sm:text-sm select-none text-center cursor-default"
      style={{ fontFamily: 'Space Grotesk, sans-serif' }}
    >
      Live Project
    </div>
  );
}
